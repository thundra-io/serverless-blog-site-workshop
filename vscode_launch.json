{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "SAM CLI Hello World",
            "type": "node",
            "request": "attach",
            "port": 5858,
            "address": "localhost",
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/var/task",
            "protocol": "inspector",
        }
    ]
}